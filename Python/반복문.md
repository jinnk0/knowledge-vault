---
created: 2026-08-07
tags: [python]
public: true
---

C나 C++와 달리 [[Python/Python|Python]]에서는 반복문 뒤에 `else` 블록을 사용할 수 있다. 반복문의 조건이 거짓이 되어 정상 종료될 때 `else` 블록이 실행된다. 단, `break` 문에 의해 반복문이 강제 종료되면 `else` 블록은 실행되지 않으며, `continue` 문에 의해서는 `else` 블록이 정상 실행된다.
