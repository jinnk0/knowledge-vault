---
created: 2026-08-12
tags: [코드품질, 추상화]
publish: true
---

[[코드 품질/코드 품질.md|코드 품질]]과 관련된 개념. 코드 작성의 목적은 문제를 해결하는 것이며, 상위 수준의 문제를 풀 때는 보통 이를 여러 하위 문제로 나눈다. 코드를 구성하는 방법은 코드 품질의 기본적인 측면 중 하나이며, 코드를 잘 구성한다는 것은 간결한 추상화 계층(layers of abstraction)을 만드는 것으로 귀결될 때가 많다.

## 왜 추상화 계층을 만드는가

코드 작성은 복잡한 문제를 계속해서 더 작은 하위 문제로 세분화하는 작업이다. 이때 이미 작성된 하위 문제의 해결책을 추상적인 개념으로 재사용해서, 상위 계층이 하위 문제의 구체적인 구현 방법을 알지 않아도 되도록 계층화하는 것이 추상화 계층이다.

간결한 추상화 계층은 상위 수준의 문제를 해결하기 위해 몇 가지 개념만 알면 된다는 것을 의미한다. 더 일반적으로 말하면, 어떤 문제를 하위 문제로 계속 나누어 내려가며 추상화 계층을 만들면 같은 층위 안에서는 쉽게 이해할 수 있는 몇 개의 개념만 다루기 때문에 개별 코드가 특별히 복잡해 보이지 않는다. 문제가 아무리 복잡하더라도 하위 문제들을 식별하고 올바른 추상화 계층을 만들면 그 복잡한 문제를 쉽게 다룰 수 있다.

## 추상화 계층과 코드 품질의 핵심 요소

깨끗하고 뚜렷한 추상화 계층을 구축하면 코드 품질의 네 가지 핵심 요소를 달성할 수 있다.

### 가독성

깨끗하고 뚜렷한 추상화 계층을 만들면 개발자가 한 번에 한두 개 정도의 계층과 몇 개의 개념만 다루면 되므로 [[코드 품질/가독성.md|가독성]]이 향상된다.

### 모듈화

추상화 계층이 하위 문제에 대한 해결책을 깔끔하게 나누고 구현 세부 사항을 외부로 노출하지 않으면, 다른 계층이나 코드의 일부에 영향을 주지 않고 계층 내에서만 구현을 바꿀 수 있게 되어 [[코드 품질/모듈화.md|모듈화]]가 향상된다.

### 재사용성과 일반화성

하위 문제에 대한 해결책이 간결한 추상화 계층으로 제시되면 그 해결책을 재사용하기 쉬워진다. 문제가 적절하게 추상적인 하위 문제로 세분화되면 해결책은 여러 상황에서 유용하게 일반화될 가능성이 크다. ([[코드 품질/재사용성과 일반화성.md|재사용성과 일반화성]] 참고)

### 테스트 용이성

코드가 추상화 계층으로 깨끗하게 분할되면 각 하위 문제에 대한 해결책을 완벽하게 테스트하기가 훨씬 쉬워진다. ([[코드 품질/테스트.md|테스트]] 참고)
